import * as React from "react";
import { LayoutChangeEvent } from "react-native";
import { DateNumber, Month, Task, User } from "../shared/model";
export interface TaskAndUser {
    user: User;
    tasks: Task[];
}
interface CalendarMonthProps {
    data?: TaskAndUser[];
}
interface CalendarMonthState {
    dateBoxWidth: number;
    selectedDate?: DateNumber;
    currentMonth: Month;
    currentYear: number;
}
interface ScheduleToUserMap {
    [key: number]: User[];
}
export declare class CalendarMonth extends React.Component<CalendarMonthProps, CalendarMonthState> {
    private boxes;
    constructor(props: CalendarMonthProps);
    generateBoxes(data: ScheduleToUserMap): JSX.Element[];
    containerOnLayout: (event: LayoutChangeEvent) => void;
    render(): JSX.Element;
    private onMonthChange;
    private onDatePress;
}
export {};
