import * as React from "react";
import { Text, View } from "react-native";
import styles from "./WeekDayHeader.styles";
export var WeekDayHeader = function () {
    var weekDays = ["S", "M", "T", "W", "T", "F", "S"];
    var weekDayHandler = function (day, index) { return (<Text key={index} style={styles.textStyle}>
            {day}
        </Text>); };
    return (<View style={styles.outerViewContainer}>
            {weekDays.map(weekDayHandler)}
        </View>);
};
//# sourceMappingURL=WeekDayHeader.js.map