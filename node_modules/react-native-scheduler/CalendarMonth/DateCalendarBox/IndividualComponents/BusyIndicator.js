import * as React from "react";
import { View } from "react-native";
import styles from "./BusyIndicator.style";
export var BusyIndicator = function (props) {
    var circleSize = props.height / 1.7;
    var circleBorderRadius = circleSize / 2;
    return (<View style={[
        styles.bottomOuterView, {
            height: props.height,
            paddingBottom: 8
        }
    ]}>
      {props
        .whosBusy
        .map(function (obj) { return <View key={obj.id} style={{
        height: circleSize,
        width: circleSize,
        borderRadius: circleBorderRadius,
        marginLeft: 2,
        marginRight: 2,
        backgroundColor: obj.colourIndicator
    }}/>; })}
    </View>);
};
//# sourceMappingURL=BusyIndicator.js.map