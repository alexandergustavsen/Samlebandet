import * as React from "react";
import { Text, View } from "react-native";
import styles from "./DateComponent.style";
var getDateTitle = function (props) {
    var dateTitle = props.date ? "" + props.date : "";
    var dateStyle = props.isWeekend ? styles.textStyleWeekend : styles.textStyleWeekday;
    return (<Text style={[dateStyle, { fontSize: props.dateFontSize }]}>
      {dateTitle}
    </Text>);
};
var getSelectedDateTitle = function (props) {
    var circleSize = props.dateComponentHeight / 1.2;
    var borderRadiusSize = circleSize / 2;
    return (<View style={[
        styles.selectedColor,
        { height: circleSize, width: circleSize, borderRadius: borderRadiusSize }
    ]}>
      <View style={styles.circleView}>
        {getDateTitle(props)}
      </View>
    </View>);
};
export var DateComponent = function (props) {
    return (<View style={styles.topOuterView}>
      {props.isSelected ? getSelectedDateTitle(props) : getDateTitle(props)}
    </View>);
};
//# sourceMappingURL=DateComponent.js.map