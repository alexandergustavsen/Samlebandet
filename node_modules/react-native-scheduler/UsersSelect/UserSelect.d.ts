import { Component } from "react";
import { UserSelection } from "../shared/model";
interface ComponentProps {
    data: UserSelection[];
    onSelectedEvent: (selectedUser: UserSelection[]) => void;
}
interface SelectedUser {
    [key: number]: UserSelection;
}
interface ComponentState {
    selectedUser: SelectedUser;
}
export declare class UserSelect extends Component<ComponentProps, ComponentState> {
    constructor(props: ComponentProps);
    renderButtons(): JSX.Element[] | null;
    selectUser: (user: UserSelection, idx: number) => void;
    render(): JSX.Element;
}
export {};
