import React, { Component } from 'react';
import { Text } from 'react-native';

export default class Test extends Component {
    render () {
        return (
            <Text>Hallo, dette er en test!</Text>
        );
    }
}